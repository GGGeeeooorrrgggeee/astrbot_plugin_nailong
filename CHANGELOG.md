# 更新记录

## v2.0.0

### 新增

- 新增配置：`静态图以 GIF 格式发送`。开启后，静态图会在发送前临时转为一帧的 GIF，更接近表情包样式，也可防止部分平台把静态图显示得过大。
- 新增普通用户指令别名：`龙来`，功能与 `来只奶龙`、`奶龙` 一致，支持随机发送和按文件名发送指定表情包。
- 新增普通用户指令：`查询奶龙数量`，可查看当前图库总数、GIF 数和静态图数。
- 新增管理员指令：`查询奶龙文件名` 和 `重命名奶龙`；`添加奶龙`、`删除奶龙`、`查询奶龙文件名` 支持批量处理表情包，`删除奶龙` 也支持按文件名删除，`重命名奶龙` 一次只重命名一张。
- 新增功能：`插件页面`。支持表情包上传、删除、重命名、批量选择、批量导出，以及从 GitHub 仓库下载奶龙图库。

### 注意

- `添加奶龙` 的去重，以及回复表情包使用 `删除奶龙`、`查询奶龙文件名`、`重命名奶龙` 时的本地文件匹配，都依赖表情包文件哈希值。开启 `静态图以 GIF 格式发送` 后，如果同一个表情包同时存在静态图版本和 GIF 动态图版本，因为文件内容不同，哈希值也不同，插件不会把它们识别成同一张表情包；同一表情包的像素尺寸、压缩方式、裁剪范围、颜色等文件内容有变化时，也可能无法通过回复表情包匹配到本地文件。
- 插件页面的 `覆盖下载` 会先清空当前本地图库和哈希记录，再导入本次下载到的表情包。

